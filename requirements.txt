# === Xử lý dữ liệu & kiểm tra data ===
pandas>=2.0.0
numpy>=1.24.0
pillow>=10.0.0

# === Tải dữ liệu ===
gdown>=5.0.0
kagglehub>=0.2.0
requests>=2.31.0
tqdm>=4.66.0

# === Model Vision-Language (zero-shot) ===
torch>=2.1.0
torchvision>=0.16.0
open_clip_torch>=2.24.0
transformers>=4.38.0

# === NLP phụ trợ (entity grounding, nếu cần) ===
spacy>=3.7.0

# === Đánh giá & phân tích ===
scikit-learn>=1.3.0
matplotlib>=3.8.0
seaborn>=0.13.0

# === Notebook (nếu chạy local bằng Jupyter thay vì Colab) ===
jupyter>=1.0.0
ipykernel>=6.29.0

# === Logging thí nghiệm (tuỳ chọn, hữu ích khi so sánh nhiều model/prompt) ===
wandb>=0.16.0

# === Đọc file yaml config ===
pyyaml>=6.0